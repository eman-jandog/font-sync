## Font Sync
Automatically install new fonts from your preferred cloud storage.

### Installation
- The default path is configured to `C:\FontSync`. You can adjust the default path but make sure to set the directory accordingly.

### How to use?
- You can hit the `exe` file or execute the `ps1` file.
- You can add a task in the Task Scheduler by importing the `xml` file.
